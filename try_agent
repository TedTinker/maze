#%%

import pickle

which_args = "d_hard"
which_epoch = 1000
agent_num = 1

with open("saved/{}/plot_dict.pickle", "rb") as handle: 
    plot_dict = pickle.load(handle)

agent_lists = plot_dict["agent_lists"]
print(agent_lists)